Title: Integrate `dice-box-threejs` and add sample scene

Description: Add a minimal 3D scene using `dice-box-threejs` to render dice and respond to roll events.

Tasks:
- [ ] Add example scene in `demo/react-project/src/three` or `src/components/DiceScene.jsx`
- [ ] Ensure deterministic seeding for demo reproducibility
- [ ] Add UI hooks to trigger rolls and show results
- [ ] Optimize performance and add simple controls (camera, reset)

Acceptance:
- Rolling in UI produces matching parser results and visual rolls

Priority: High
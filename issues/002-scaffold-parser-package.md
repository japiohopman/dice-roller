Title: Scaffold `dice-parser` package (packages/dice-parser)

Description: Create a small, well-documented package implementing the parser interface for dice notation.

Tasks:
- [ ] Add package skeleton (package.json, README, tests folder)
- [ ] Define the `dice-parser-interface` contract (input, output, error model)
- [ ] Implement a basic parser handling common dice notation (e.g., `1d6+2`, `4d8k2`)
- [ ] Add unit tests and CI lint/test job
- [ ] Add example usage and integration in demo

Acceptance:
- Parser exposes a documented API and passes unit tests

Priority: High